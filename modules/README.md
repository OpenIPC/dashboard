# External Modules

The analytics modules have moved to dedicated repositories and are no longer bundled with the core application. This directory is intentionally left empty so the module manager can download modules on-demand into the user data directory.
